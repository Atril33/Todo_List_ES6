import './style.css';
import './TodoList.js';

/* const myTodo = []; */

/* const myList = document.getElementById('listholder');
for (let i = 0; i < myTodo.length; i += 1) {
  const li = document.createElement('li');
  li.innerHTML = `<li class="my-list"><input type="checkbox" class="checkbox"
   ${myTodo[i].completed ?
    'checked' : ''}>
    <p>${myTodo[i].description}</p></li>`;
  myList.appendChild(li);
}
 */
